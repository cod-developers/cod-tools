Tests the way data items from the ATOM_TYPE loop are handled.
The loop should only be output it both the '_atom_type.symbol'
and the '_atom_type.oxidation_number' data items are present
in the input file.
