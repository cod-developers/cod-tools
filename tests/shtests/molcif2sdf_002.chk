#!/bin/sh

BABEL_FORMAT=$( babel -V | perl -ne 'm/^Open Babel (\d+[.]\d+[.]\d+)/; print $1' )

if ! [ "$BABEL_FORMAT" = '2.3.0' ]
then
    echo Test skipped - test only compatible with Open Babel version 2.3.0
    exit 1
fi
