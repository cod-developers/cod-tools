SPGLIB_DIR  = spglib
SPGLIB_MAKE = ${SPGLIB_DIR}/Makefile

all check test tests out output outputs listdiff: ${SPGLIB_MAKE}

${SPGLIB_MAKE}:
	(cd ${SPGLIB_DIR}; ./configure)

# clean cleanAll distclean:
# 	test -e ${SPGLIB_MAKE} && ${MAKE} -C ${SPGLIB_DIR} $@
