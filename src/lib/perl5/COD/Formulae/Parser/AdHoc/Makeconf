PKG_PREFIX = COD::Formulae::Parser::
PKG_PATH = ${patsubst %/,%, ${subst ::,/,${PKG_PREFIX}}}

PKG_TOP   = lib

CWD = ${notdir $(shell pwd)}
TEST_MODULE = ./${PKG_TOP}/${PKG_PATH}/${CWD}.pm

PERL5LIB := ./${PKG_TOP}:${PERL5LIB}
export PERL5LIB
