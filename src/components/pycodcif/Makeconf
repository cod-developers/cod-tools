PKG_LIB_DIR = usr/local/lib/python2.7/dist-packages/pycodcif

CWD = ${notdir $(shell pwd)}
TEST_MODULE = ${PKG_LIB_DIR}/${CWD}.py
TEST_DEPEND = ${PKG_LIB_DIR}/_${CWD}.so

PYTHONPATH := ${PKG_LIB_DIR}:${PYTHONPATH}
export PYTHONPATH

PREFIX = /usr
